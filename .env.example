# Pocket Terminal configuration

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------
# The password required to access the terminal UI.
# IMPORTANT:
# - In PRODUCTION you MUST set this variable. If it is not set in production,
#   the server will refuse all logins and the UI will show a clear message:
#   "Server authentication is not configured. Admin must set TERMINAL_PASSWORD."
# - In NON-PRODUCTION (e.g. local development), if this variable is missing,
#   the app falls back to a default password: Superprimitive69!
#   This is for convenience only and must not be relied on in production.
#
# Example:
# TERMINAL_PASSWORD=my-strong-password
TERMINAL_PASSWORD=

# ---------------------------------------------------------------------------
# Session configuration
# ---------------------------------------------------------------------------
# Session TTL (time-to-live) in milliseconds.
# Default: 7 days (1000 * 60 * 60 * 24 * 7)
SESSION_TTL_MS=604800000

# ---------------------------------------------------------------------------
# Server configuration
# ---------------------------------------------------------------------------
# Port for the HTTP/WebSocket server to listen on.
PORT=3000

# Node environment
# Set NODE_ENV=production on your production deployment.
NODE_ENV=development