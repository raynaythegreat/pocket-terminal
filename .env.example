# Pocket Terminal environment example
# -------------------------------------------------
# REQUIRED: Set a strong password for terminal access.
# The application will hash this value (SHA‑256) at runtime,
# so the plain password never appears in logs or source control.
TERMINAL_PASSWORD=your-strong-password-here

# Optional: custom port (defaults to 3000)
PORT=3000