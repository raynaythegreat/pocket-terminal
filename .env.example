# Pocket Terminal configuration

# Port the HTTP server listens on
PORT=3000

# Node environment: "development" or "production"
NODE_ENV=development

# Authentication password.
# In PRODUCTION:
#   - TERMINAL_PASSWORD MUST be set, or no logins will be allowed.
# In NON-PRODUCTION:
#   - If not set, the default password "Superprimitive69!" is used.
#     This is for local development only; do NOT rely on this in production.
TERMINAL_PASSWORD=

# Session TTL in milliseconds (default: 7 days)
# Example: 86400000 for 1 day
SESSION_TTL_MS=604800000

# Dedicated HOME for CLI tools (auth/config/history)
CLI_HOME_DIR=./workspace/cli-home
